import React from 'react';
import { render } from 'react-dom';
import StorePicker from './components/StorePicker.js'

render(<StorePicker />, document.querySelector('#main'));

// Left off on video 5 - Loading CSS into React app